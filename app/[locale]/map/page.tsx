import { MapView } from '@/components/map/map-view';

/**
 * /map page — renders the full-viewport interactive map.
 * Layout (sidebar + navbar) is provided by layout.tsx.
 */
export default function MapPage() {
	return <MapView />;
}
